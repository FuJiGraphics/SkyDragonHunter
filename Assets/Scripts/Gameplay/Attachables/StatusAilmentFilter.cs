using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SkyDragonHunter {
    public class StatusAilmentFilter : MonoBehaviour
    {
        // 필드 (Fields)
        public bool isBurnImmune = false;
        public bool isFreezeImmune = false;
        public bool isSlowImmune = false;

        // 속성 (Properties)
        // 외부 종속성 필드 (External dependencies field)
        // 이벤트 (Events)
        // 유니티 (MonoBehaviour 기본 메서드)
        // Public 메서드
        // Private 메서드
        // Others
    
    } // Scope by class StatusAilmentFilter
} // namespace SkyDragonHunter